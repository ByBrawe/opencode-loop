import { define } from "@opencode-ai/plugin/v2/promise"

export const OPENCODE2_LOOP_PLUGIN_ID = "bybrawe.opencode-loop.v2"

export default define({
  id: OPENCODE2_LOOP_PLUGIN_ID,
  setup: async () => {},
})
