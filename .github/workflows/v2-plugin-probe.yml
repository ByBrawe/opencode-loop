name: OpenCode 2 Plugin Probe

on:
  push:
    branches: [v2-real-host-sentinel]

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: actions/setup-node@v6
        with:
          node-version: "24"
          cache: npm
      - run: npm ci
      - run: npm install --no-save --package-lock=false @opencode-ai/cli@next @opencode-ai/plugin@next
      - name: Inspect loaded V2 plugins with debug logs
        working-directory: scripts/fixtures/v2-project
        run: ../../../node_modules/.bin/opencode2 --print-logs --log-level DEBUG api --standalone get /api/plugin
