name: OpenCode 2 Activation

on:
  pull_request:
    paths:
      - "src/source/opencode2/**"
      - ".github/workflows/opencode2-activation.yml"
  push:
    branches:
      - main
      - v2-real-host-plugin-list
    paths:
      - "src/source/opencode2/**"
      - ".github/workflows/opencode2-activation.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  activation:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false

      - uses: actions/setup-node@v6
        with:
          node-version: "24"
          cache: npm

      - name: Initialize isolated OpenCode 2 paths
        shell: bash
        run: |
          echo "HOME=$RUNNER_TEMP/opencode2-home" >> "$GITHUB_ENV"
          echo "XDG_CONFIG_HOME=$RUNNER_TEMP/opencode2-home/.config" >> "$GITHUB_ENV"
          echo "XDG_DATA_HOME=$RUNNER_TEMP/opencode2-home/.local/share" >> "$GITHUB_ENV"
          echo "XDG_STATE_HOME=$RUNNER_TEMP/opencode2-home/.local/state" >> "$GITHUB_ENV"

      - run: npm ci

      - name: Install matching OpenCode 2 beta packages
        run: npm install --no-save --package-lock=false @opencode-ai/cli@next @opencode-ai/plugin@next

      - name: Show tested V2 versions and package layout
        shell: bash
        run: |
          node -p "require('./node_modules/@opencode-ai/plugin/package.json').version"
          "$GITHUB_WORKSPACE/node_modules/.bin/opencode2" --version
          node -p "JSON.stringify(require('./node_modules/@opencode-ai/plugin/package.json').exports, null, 2)"
          find "$GITHUB_WORKSPACE/node_modules/@opencode-ai/plugin/dist" -maxdepth 4 -type f -print | sort || true

      - name: Prepare dependency-free V2 activation probe
        shell: bash
        run: |
          mkdir -p "$HOME" "$XDG_CONFIG_HOME" "$XDG_DATA_HOME" "$XDG_STATE_HOME"
          project="$RUNNER_TEMP/opencode-loop-v2-activation"
          plugin_dir="$project/.opencode/plugins"
          mkdir -p "$plugin_dir"
          git init --quiet "$project"
          git -C "$project" config user.email "opencode-loop-ci@example.invalid"
          git -C "$project" config user.name "OpenCode Loop CI"
          printf '%s\n' 'export default { id: "bybrawe.opencode-loop.v2.activation-probe", async setup() {} }' > "$plugin_dir/opencode-loop-v2-probe.js"
          git -C "$project" add .opencode/plugins/opencode-loop-v2-probe.js
          git -C "$project" commit --quiet -m "init"
          cat "$plugin_dir/opencode-loop-v2-probe.js"

      - name: Probe V2 plugin activation after service startup
        shell: bash
        run: |
          cd "$RUNNER_TEMP/opencode-loop-v2-activation"
          echo "--- first plugin list ---"
          "$GITHUB_WORKSPACE/node_modules/.bin/opencode2" plugin list
          sleep 1
          echo "--- second plugin list ---"
          "$GITHUB_WORKSPACE/node_modules/.bin/opencode2" plugin list

      - name: Dump OpenCode 2 logs on failure
        if: failure()
        shell: bash
        run: |
          find "$XDG_DATA_HOME" -maxdepth 6 -type f -print
